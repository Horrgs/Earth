import matplotlib as mpl
import matplotlib.pyplot as plt
import numpy


def to_graph_data(data):  # method to take data and make it compatible with matplotlib accepted data types.
    pass


def create_x_plot(titles, data):
    pass


def add_graph(titles, data, plot): # plots another graph (i.e. a line) on an existing plot (with a graph/line likely present)
    pass

